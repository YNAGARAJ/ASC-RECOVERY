# PHI and secrets — never commit
data/real/
phi/
secrets/
*.pem
*.key
*.835
*.edi

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.venv/
venv/
.env
.env.*
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local eval run history (evals/history.py) -- a rolling local log of
# `make eval` invocations, not meaningful shared history across machines;
# a real deployment sends this to a metrics backend instead.
evals/history/
